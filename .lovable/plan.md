Plan to update the website color theme

1. Replace the purple base palette with near-black charcoal
- Change the global background from purple to a deep charcoal/black tone.
- Update card, panel, muted, input, border, and sidebar colors so the whole site feels cohesive instead of purple-tinted.
- Keep the existing red accent as the main brand highlight for buttons, shadows, labels, and scan effects.

2. Adjust supporting contrast colors
- Use off-white/light gray for primary text.
- Use medium gray for secondary text.
- Make borders and panels subtle charcoal/gray with red highlights where needed.
- Keep the pixel/arcade feel without the purple cast.

3. Update visual effects to match
- Change the grid scan line color from purple to charcoal/gray while keeping the red scan color.
- Adjust global glow/radial background effects so they use red over charcoal instead of red over purple.
- Preserve the scanline and retro pixel styling.

4. Verify mobile and runtime stability
- Check the home, work, and contact pages at the current mobile viewport size.
- Fix any obvious loading/runtime issue encountered during verification, including the dynamic import error if it is caused by the current project state.
- Ensure buttons, panels, headings, and gallery images remain readable on mobile.

Technical details
- Main color work will be in `src/styles.css` by changing the CSS variables in `:root` and `.dark`.
- The animated background adjustment will be in `src/components/GridScanBackground.tsx`.
- I will not edit `src/routeTree.gen.ts` directly because it is auto-generated by TanStack Start.